Read the file AGENTS.md to understand structure of the project and expectations for AI agents

<!-- TODO: Include in CLAUDE.md overhaul —
  E2E tests (cd e2e && npm test) run locally. Never skip test gates by assuming
  infrastructure isn't available — always attempt the command and let the result
  determine pass/fail. This bit a Ralph loop agent that skipped the Phase 3 e2e
  gate without trying.
-->